# Demo resource provenance

The files under `Resources/` are copied from the `examples/` directory of
`KentLee86/cetz-renderer`. Both repositories are MIT licensed and owned by the
same author. Rendered PDF, PNG, and SVG previews are intentionally excluded;
only source files and source-referenced assets are embedded in this shared demo
assembly.
